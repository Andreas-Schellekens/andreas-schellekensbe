// Temporary fallback typings for the installed `three` package build.
// This keeps TypeScript builds unblocked when declaration files are not present.
// Prefer installing `@types/three` when your local package manager is available.
declare module "three";
